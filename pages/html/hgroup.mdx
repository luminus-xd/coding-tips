import { Callout } from "nextra/components";

# hgroup

現在は、見出し要素と p 要素のグループ化に使用されています。

```html
<hgroup>
  <h1>Heading</h1>
  <p>Description</p>
</hgroup>
```

<Callout>
  以前までは、hgroup
  要素は、h1〜h6要素のグループ化に使用されていましたが、現在は非推奨となっています。
</Callout>
